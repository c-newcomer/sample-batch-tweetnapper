package servlet; 

import org.junit.Test;

import application.ControllerBean; 
import servlet.EndpointHelper;

public class MapServletTest {
    private static final long serialVersionUID = 1L;
    @Test
    public void testDeployment() {
        System.out.println("MapServletIT is running!");
        // MapServlet ms = new MapServlet(); 
        // ControllerBean overlord = new ControllerBean();
        // System.out.println(overlord.printStuff());

        // EndpointHelper helper = new EndpointHelper();
        // helper.testEndpoint("/", "Hello, from a Servlet!");
    }
}